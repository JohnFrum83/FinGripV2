import SwiftUI

struct AccountDetailsView: View {
    var body: some View {
        Text("Account Details")
            .navigationTitle(Text("Account Details"))
    }
}

#Preview {
    AccountDetailsView()
} 