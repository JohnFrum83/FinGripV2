import SwiftUI

struct BankConnectionView: View {
    var body: some View {
        Text("Bank Connection")
            .navigationTitle(Text("Bank Connection"))
    }
}

#Preview {
    BankConnectionView()
} 